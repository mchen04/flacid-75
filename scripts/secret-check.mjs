import{readFile,writeFile}from'node:fs/promises';import{execFileSync}from'node:child_process';
const vars=Object.fromEntries((await readFile('.env.local','utf8')).split('\n').filter(l=>l.includes('=')).map(l=>[l.slice(0,l.indexOf('=')),l.slice(l.indexOf('=')+1)]));
const secrets=Object.values(vars).filter(v=>v.length>10);if(vars.DATABASE_URL)secrets.push(decodeURIComponent(new URL(vars.DATABASE_URL).password));const findings=[];
const files=execFileSync('git',['ls-files','-z'],{encoding:'utf8'}).split('\0').filter(Boolean);for(const path of files){try{const b=await readFile(path);if(secrets.some(s=>b.includes(s)))findings.push({where:'working tree',path});}catch{}}
const ids=execFileSync('git',['rev-list','--objects','HEAD'],{encoding:'utf8'}).trim().split('\n');let blobs=0;for(const item of ids){const [id,...rest]=item.split(' ');if(execFileSync('git',['cat-file','-t',id],{encoding:'utf8'}).trim()!=='blob')continue;blobs++;const b=execFileSync('git',['cat-file','-p',id],{maxBuffer:20000000});if(secrets.some(s=>b.includes(s)))findings.push({where:'history',path:rest.join(' ')});}
const manifest=JSON.parse(await readFile('public/client-assets.json','utf8'));const client=await readFile('public'+manifest.script);if(secrets.some(s=>client.includes(s)))findings.push({where:'client bundle',path:manifest.script});const result={trackedFiles:files.length,historyBlobs:blobs,clientBytes:client.length,findings};await writeFile('evidence/secret-check.json',JSON.stringify(result,null,2));console.log(result);if(findings.length)process.exitCode=1;
